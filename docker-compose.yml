services:
  api:
    build: .
    image: kuru-backend:local
    container_name: kuru-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - kuru-data:/app/data
    env_file:
      - .env.prod

volumes:
  kuru-data:
